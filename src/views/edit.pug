extends base.pug

block content 
    h4 Change Title of video
        form(method="post")
            input(name="title",placeholder="Write Video Title",value=video.title,required)
            input(value="Save",type="submit")

